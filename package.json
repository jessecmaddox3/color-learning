{
  "name": "color-learning",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "node scripts/build.mjs",
    "start": "npm run build && node scripts/serve.mjs",
    "test:sql": "python3 scripts/test-cloud-schema.py",
    "build:colors": "node data/build-colors.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.116.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "fake-indexeddb": "6.2.5"
  }
}
